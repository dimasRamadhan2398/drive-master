meta {
  name: Confirm Reset Password
  type: http
  seq: 1
}

post {
  url: {{user_service_url}}/api/v1/auth/confirm-reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "reset-token-here",
    "new_password": "newpassword123"
  }
}
