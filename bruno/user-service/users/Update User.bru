meta {
  name: Update User
  type: http
  seq: 5
}

put {
  url: {{user_service_url}}/api/v1/users/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{user_id}}
}

body:json {
  {
    "name": "Updated Name",
    "phone": "089876543210"
  }
}
