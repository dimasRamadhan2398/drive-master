meta {
  name: Get User By ID
  type: http
  seq: 4
}

get {
  url: {{user_service_url}}/api/v1/users/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{user_id}}
}
