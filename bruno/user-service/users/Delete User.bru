meta {
  name: Delete User
  type: http
  seq: 2
}

delete {
  url: {{user_service_url}}/api/v1/users/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{user_id}}
}
