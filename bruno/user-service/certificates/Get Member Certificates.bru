meta {
  name: Get Member Certificates
  type: http
  seq: 5
}

get {
  url: {{user_service_url}}/api/v1/certificates/member/:memberId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  memberId: {{member_id}}
}
