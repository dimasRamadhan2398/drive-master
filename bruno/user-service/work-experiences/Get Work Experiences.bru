meta {
  name: Get Work Experiences
  type: http
  seq: 3
}

get {
  url: {{user_service_url}}/api/v1/instructors/:id/work-experiences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{instructor_id}}
}
