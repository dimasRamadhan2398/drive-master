meta {
  name: Update Work Experience
  type: http
  seq: 4
}

put {
  url: {{user_service_url}}/api/v1/work-experiences/:expId
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  expId: 1
}

body:json {
  {
    "company": "Updated Company",
    "position": "Lead Instructor"
  }
}
