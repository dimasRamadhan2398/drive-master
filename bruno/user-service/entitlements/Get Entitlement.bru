meta {
  name: Get Entitlement
  type: http
  seq: 4
}

get {
  url: {{user_service_url}}/api/v1/entitlements/members/:id/entitlements/:entId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{member_id}}
  entId: 1
}
