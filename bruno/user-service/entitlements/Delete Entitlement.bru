meta {
  name: Delete Entitlement
  type: http
  seq: 2
}

delete {
  url: {{user_service_url}}/api/v1/entitlements/members/:id/entitlements/:entId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{member_id}}
  entId: 1
}
