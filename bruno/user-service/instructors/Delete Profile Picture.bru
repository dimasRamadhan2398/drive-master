meta {
  name: Delete Profile Picture
  type: http
  seq: 3
}

delete {
  url: {{user_service_url}}/api/v1/instructors/:id/media
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{instructor_id}}
}
