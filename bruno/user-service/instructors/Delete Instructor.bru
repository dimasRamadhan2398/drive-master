meta {
  name: Delete Instructor
  type: http
  seq: 2
}

delete {
  url: {{user_service_url}}/api/v1/instructors/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: {{instructor_id}}
}
