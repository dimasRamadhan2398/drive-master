meta {
  name: Get Member Certificates
  type: http
  seq: 3
}

get {
  url: {{user_service_url}}/api/v1/members/:userId/certificates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  userId: {{user_id}}
}
