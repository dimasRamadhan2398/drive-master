meta {
  name: Download Member Certificate
  type: http
  seq: 1
}

get {
  url: {{user_service_url}}/api/v1/members/:userId/certificates/:certId/download
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  userId: {{user_id}}
  certId: 1
}
