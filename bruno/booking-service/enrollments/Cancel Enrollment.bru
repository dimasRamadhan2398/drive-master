meta {
  name: Cancel Enrollment
  type: http
  seq: 1
}

post {
  url: {{booking_service_url}}/api/v1/enrollments/:id/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: 1
}
