meta {
  name: Create Payment
  type: http
  seq: 2
}

post {
  url: {{booking_service_url}}/api/v1/payments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "enrollment_id": 1,
    "amount": 1500000,
    "payment_method": "bank_transfer"
  }
}
