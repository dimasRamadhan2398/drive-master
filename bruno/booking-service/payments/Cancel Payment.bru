meta {
  name: Cancel Payment
  type: http
  seq: 1
}

post {
  url: {{booking_service_url}}/api/v1/payments/order/:orderId/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  orderId: ORD-001
}
