meta {
  name: Start Session
  type: http
  seq: 11
}

post {
  url: {{booking_service_url}}/api/v1/schedules/:id/start
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: 1
}
