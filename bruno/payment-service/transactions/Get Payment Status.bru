meta {
  name: Get Payment Status
  type: http
  seq: 5
}

get {
  url: {{payment_service_url}}/api/v1/payments/:id/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  id: 1
}
